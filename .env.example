# Database Configuration
DB_SERVER=your-sql-server-host
DB_NAME=your-database-name
DB_USER=your-db-username
DB_PASSWORD=your-db-password
DB_DRIVER=ODBC Driver 17 for SQL Server

# ============================================
# LLM Provider Configuration
# ============================================
# Choose your LLM provider: "vertexai" or "ollama"
LLM_PROVIDER=vertexai

# --- Vertex AI Configuration (when LLM_PROVIDER=vertexai) ---
# Google Cloud API Key (using official genai SDK)
GOOGLE_CLOUD_API_KEY=your-google-cloud-api-key

# Vertex AI Model
VERTEX_MODEL=gemini-2.5-flash
# Alternative models: gemini-2.0-flash-exp, gemini-pro, gemini-1.5-flash

# --- Ollama Configuration (when LLM_PROVIDER=ollama) ---
# Ollama Server URL
OLLAMA_BASE_URL=http://localhost:11434

# Ollama Model
OLLAMA_MODEL=llama3.1:8b
# Alternative models: llama3.1:70b, qwen2.5:32b, mistral:7b, deepseek-coder:33b

# Application Configuration
APP_HOST=0.0.0.0
APP_PORT=8000
